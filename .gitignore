# Tool-internal scratch dirs (Claude Code / Playwright MCP)
.claude/
.playwright-mcp/
.superpowers/

# Stray screenshot artifacts from browser-driven testing (repo root only;
# experiment subdirs can still commit PNGs as legitimate assets).
/*.png

# Interstellar Tourism — Vite build artifacts
interstellar-tourism/node_modules/
interstellar-tourism/dist/

# Legacy standalone sky-color-gl dir (subtree-merged into
# interstellar-tourism/), kept on disk pending user direction.
/sky-color-gl/
