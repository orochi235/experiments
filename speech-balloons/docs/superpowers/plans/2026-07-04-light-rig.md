# User-Editable Light Rig Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Replace the hardcoded two-light rig with a scene-level, user-editable light list (panel + on-canvas gizmos), consumed by all four fill modes.

**Architecture:** `DesignState` gains `lights: LightInstance[]` (min 1, max 6). A pure `lightRig.ts` module owns defaults + localStorage migration; `SpeechBalloon` reads `design.lights` instead of the `domeLights` memo; a new `LightsPanel` block edits the list; gizmo interaction reuses the debug overlay's lollipop projection with an inverse solver in `lightGizmo.ts`.

**Tech Stack:** React 18 + TypeScript, `@weasel-js/labkit` (PropertyPanel/SliderRow/ColorRow), vitest, vite dev server. Spec: `docs/superpowers/specs/2026-07-03-light-rig-design.md`.

**Working directory:** `~/src/experiments/speech-balloons` (all paths below relative to it). Run tests with `npx vitest run <file>`, typecheck with `npm run typecheck`. Commits happen from the repo root (`~/src/experiments`); prefix paths with `speech-balloons/`.

**Key existing facts:**
- `domeLights` memo at `src/SpeechBalloon.tsx:975-982` builds `[{az: keyAz, el: keyEl, intensity: 1.0}, {az: (keyAz+180)%360, el: 25, intensity: 0.35}]` from fill params. Consumed by: `litBevel` memo (:1026), `domeLayers` (:1070), `brdfLayers` (:1142, already loops all lights), `debugOverlay` (:1355).
- Aqua reads `fillRender.lightAngle` (:944). Lit-bevel forces light 1 white (:1037).
- Fill param descriptors to delete live in `src/controls.ts`: `lightAngle` (:37), `lightAzimuth` (:49), `lightElevation` (:50), `lightColor` (:71).
- Undo is a debounced snapshot-on-settle in `Lab.tsx:208-226` — continuous `setDesign` calls during a drag coalesce into one undo step automatically.
- Migration machinery: `migrateWorkspaces()` in `src/main.tsx:74-96` mutates stored workspace `config` objects in place, guarded by a `dirty` flag.
- `Lab.tsx` renders `<SpeechBalloon design={design} runtime={runtime} …>` at :674; right panel (`sb-side-panel-right`) starts at :701.

---

### Task 1: `lightRig.ts` — types, defaults, migration (pure logic)

**Files:**
- Modify: `src/types.ts`
- Create: `src/lightRig.ts`
- Create: `src/lightRig.test.ts`

- [ ] **Step 1: Add the type to `src/types.ts`**

After the `ParamBag` type (line 7), add:

```ts
export interface LightInstance {
  az: number;        // degrees, 0..359
  el: number;        // degrees, 0..90
  intensity: number; // 0..2
  color: string;     // hex
}
```

In `DesignState`, after `effects: EffectInstance[];` add:

```ts
  lights: LightInstance[]; // scene light rig, min 1 max 6 (see lightRig.ts)
```

In `RuntimeState`, after `hiddenShadingIds?: string[];` add:

```ts
  // Light gizmo visibility (view preference — not undoable design state).
  // undefined means true.
  showLightHandles?: boolean;
```

- [ ] **Step 2: Write failing tests in `src/lightRig.test.ts`**

```ts
import { describe, expect, it } from 'vitest';
import { defaultLights, ensureLights, MAX_LIGHTS, MIN_LIGHTS, newLight } from './lightRig';

describe('defaultLights', () => {
  it('reproduces the old hardcoded pair', () => {
    expect(defaultLights()).toEqual([
      { az: 270, el: 55, intensity: 1.0, color: '#ffffff' },
      { az: 90, el: 25, intensity: 0.35, color: '#ffffff' },
    ]);
  });
  it('returns a fresh array each call', () => {
    expect(defaultLights()).not.toBe(defaultLights());
  });
});

describe('constants', () => {
  it('bounds', () => {
    expect(MIN_LIGHTS).toBe(1);
    expect(MAX_LIGHTS).toBe(6);
    expect(newLight()).toEqual({ az: 90, el: 45, intensity: 0.5, color: '#ffffff' });
  });
});

describe('ensureLights (workspace config migration)', () => {
  it('synthesizes the rig from dome/brdf/lit-bevel fill params and strips them', () => {
    const config: Record<string, unknown> = {
      effects: [
        { id: 1, kind: 'fill', params: { mode: 'lit-bevel', lightAzimuth: 300, lightElevation: 40, lightColor: '#ff0000', lightAngle: 15 } },
      ],
    };
    expect(ensureLights(config)).toBe(true);
    expect(config.lights).toEqual([
      { az: 300, el: 40, intensity: 1.0, color: '#ff0000' },
      { az: 120, el: 25, intensity: 0.35, color: '#ffffff' },
    ]);
    const params = (config.effects as Array<{ params: Record<string, unknown> }>)[0]!.params;
    expect(params.lightAzimuth).toBeUndefined();
    expect(params.lightElevation).toBeUndefined();
    expect(params.lightColor).toBeUndefined();
    expect(params.lightAngle).toBeUndefined();
  });

  it('uses lightAngle for aqua fills', () => {
    const config: Record<string, unknown> = {
      effects: [{ id: 1, kind: 'fill', params: { mode: 'aqua', lightAngle: 30, lightAzimuth: 300 } }],
    };
    ensureLights(config);
    expect((config.lights as Array<{ az: number }>)[0]!.az).toBe(30);
  });

  it('falls back to defaults when params are absent or there is no fill', () => {
    const noParams: Record<string, unknown> = { effects: [{ id: 1, kind: 'fill', params: {} }] };
    ensureLights(noParams);
    expect(noParams.lights).toEqual(defaultLights());

    const noFill: Record<string, unknown> = { effects: [{ id: 2, kind: 'tail', params: {} }] };
    ensureLights(noFill);
    expect(noFill.lights).toEqual(defaultLights());
  });

  it('is a no-op when lights already exist (returns false, params untouched)', () => {
    const config: Record<string, unknown> = {
      lights: [{ az: 10, el: 20, intensity: 0.5, color: '#00ff00' }],
      effects: [{ id: 1, kind: 'fill', params: { lightAzimuth: 300 } }],
    };
    expect(ensureLights(config)).toBe(false);
    expect((config.lights as unknown[]).length).toBe(1);
    // Old params survive untouched when we didn't migrate — never half-migrate.
    expect((config.effects as Array<{ params: Record<string, unknown> }>)[0]!.params.lightAzimuth).toBe(300);
  });
});
```

- [ ] **Step 3: Run tests, confirm they fail**

Run: `npx vitest run src/lightRig.test.ts`
Expected: FAIL — `Cannot find module './lightRig'` (or equivalent).

- [ ] **Step 4: Implement `src/lightRig.ts`**

```ts
// Scene light rig: defaults, bounds, and the localStorage migration that
// lifts pre-rig fill params (lightAzimuth/lightElevation/lightColor/
// lightAngle) into DesignState.lights. Spec:
// docs/superpowers/specs/2026-07-03-light-rig-design.md
import type { LightInstance } from './types';

export const MIN_LIGHTS = 1;
export const MAX_LIGHTS = 6;

// Matches the old hardcoded pair in SpeechBalloon exactly, so migrated and
// fresh scenes render pixel-identical to the pre-rig renderer.
export function defaultLights(): LightInstance[] {
  return [
    { az: 270, el: 55, intensity: 1.0, color: '#ffffff' },
    { az: 90, el: 25, intensity: 0.35, color: '#ffffff' },
  ];
}

export function newLight(): LightInstance {
  return { az: 90, el: 45, intensity: 0.5, color: '#ffffff' };
}

/**
 * In-place workspace-config migration. Synthesizes `config.lights` from the
 * first fill effect's legacy light params and deletes those params.
 * Returns true when it changed anything (caller persists only when dirty).
 */
export function ensureLights(config: Record<string, unknown>): boolean {
  if (Array.isArray(config.lights)) return false;

  const effects = Array.isArray(config.effects)
    ? (config.effects as Array<{ kind?: string; params?: Record<string, unknown> }>)
    : [];
  const fill = effects.find((e) => e?.kind === 'fill');
  const p = fill?.params ?? {};

  const isAqua = p.mode === 'aqua';
  const azRaw = isAqua ? p.lightAngle : p.lightAzimuth;
  const az = typeof azRaw === 'number' ? azRaw : 270;
  const el = typeof p.lightElevation === 'number' && !isAqua ? p.lightElevation : 55;
  const color = typeof p.lightColor === 'string' ? p.lightColor : '#ffffff';

  config.lights = [
    { az, el, intensity: 1.0, color },
    { az: (az + 180) % 360, el: 25, intensity: 0.35, color: '#ffffff' },
  ] satisfies LightInstance[];

  if (fill?.params) {
    delete fill.params.lightAzimuth;
    delete fill.params.lightElevation;
    delete fill.params.lightColor;
    delete fill.params.lightAngle;
  }
  return true;
}
```

- [ ] **Step 5: Run tests, confirm they pass**

Run: `npx vitest run src/lightRig.test.ts`
Expected: PASS (7 tests). Note: `npm run typecheck` will FAIL right now — `initialDesign()` doesn't provide `lights` yet. That's Task 2; don't chase it here.

- [ ] **Step 6: Commit**

```bash
git add speech-balloons/src/types.ts speech-balloons/src/lightRig.ts speech-balloons/src/lightRig.test.ts
git commit -m "feat(speech-balloons): LightInstance type + lightRig defaults/migration"
```

---

### Task 2: Wire rig into initial state and localStorage migration

**Files:**
- Modify: `src/initialState.ts`
- Modify: `src/main.tsx:74-96` (`migrateWorkspaces`) and `main.tsx:24-68` (`migrateStorage`)

- [ ] **Step 1: `src/initialState.ts` — seed new designs**

Add the import and the field:

```ts
import { defaultLights } from './lightRig';
```

In `initialDesign()`, after `effects: initialEffects(),`:

```ts
    lights: defaultLights(),
```

- [ ] **Step 2: `src/main.tsx` — run `ensureLights` over stored workspaces**

Add the import:

```ts
import { ensureLights } from './lightRig';
```

In `migrateWorkspaces()`, inside the `for (const ws of workspaces)` loop after the `lean → shear` block, add:

```ts
      // Synthesize the scene light rig from legacy fill params.
      if (ensureLights(config)) dirty = true;
```

In `migrateStorage()` (the pre-labkit path), after the `lean → shear` block (line ~53), add:

```ts
    ensureLights(design);
```

(No dirty flag needed there — that path always writes the workspace record.)

- [ ] **Step 3: Typecheck + full suite**

Run: `npm run typecheck && npx vitest run`
Expected: typecheck clean (the `lights` field now exists everywhere `DesignState` is constructed — `initialDesign` is the only literal constructor; undo/export round-trip whole objects). All existing tests pass (125 + 7 new).

If typecheck flags other `DesignState` literals (e.g. in tests), add `lights: defaultLights()` to them the same way.

- [ ] **Step 4: Commit**

```bash
git add speech-balloons/src/initialState.ts speech-balloons/src/main.tsx
git commit -m "feat(speech-balloons): seed + migrate scene light rig in state layer"
```

---

### Task 3: Renderer consumes `design.lights`

**Files:**
- Modify: `src/SpeechBalloon.tsx` (fillRender :892-938, aquaPaint :942, domeLights :975-982, litBevel :1026-1068)
- Modify: `src/controls.ts` (:37, :49, :50, :71)
- Modify: `src/litBevelShading.test.ts` (add a per-light-color case)

- [ ] **Step 1: Write the failing per-light-color test**

In `src/litBevelShading.test.ts`, add (adapting to the file's existing region/contour fixtures — it already builds `Region` objects and calls `computeStops`; copy the fixture style of the nearest existing test):

```ts
it('tints each light with its own color', () => {
  const lights = [
    { az: 270, el: 55, intensity: 1, color: '#ff0000' },
    { az: 90, el: 55, intensity: 1, color: '#0000ff' },
  ];
  // Pure-ambient-off material, white albedo, no specular: stop colors must
  // pick up red from one flank and blue from the other rather than
  // rendering both lights white.
  const material = {
    base: '#ffffff', heightPx: 10, dMaxPx: 10, diffuse: 1,
    specular: 0, shininess: 30, specularColor: '#ffffff', ambient: 0,
  };
  const west = computeStops(stripRegion(180), lights, (x) => x, material);
  const east = computeStops(stripRegion(0), lights, (x) => x, material);
  // stripRegion(azimuthDeg) — reuse/adapt the file's existing region fixture.
  const red = (s: { color: string }) => parseInt(s.color.slice(1, 3), 16);
  const blue = (s: { color: string }) => parseInt(s.color.slice(5, 7), 16);
  const westEnd = west[west.length - 1]!;
  const eastEnd = east[east.length - 1]!;
  expect(red(westEnd)).toBeGreaterThan(blue(westEnd));   // west flank: red key dominates
  expect(blue(eastEnd)).toBeGreaterThan(red(eastEnd));   // east flank: blue fill dominates
});
```

This test passes against `computeStops` as-is (the engine already supports per-light color) — it exists to pin the behavior the renderer wiring is about to start using. If the file has no reusable strip fixture, build one: `{ kind: 'strip', azimuthDeg, x0: 0, x1: 1, frame: { kind: 'gradient', ... } }` matching the `Region` type in `bevelRegions.ts`.

- [ ] **Step 2: Run it**

Run: `npx vitest run src/litBevelShading.test.ts`
Expected: PASS (engine already supports this). If it fails, the fixture doesn't match `Region` — fix the fixture, not the engine.

- [ ] **Step 3: Rewire `SpeechBalloon.tsx`**

1. Import: `import { defaultLights } from './lightRig';` and `import type { LightInstance } from './types';`

2. Replace the `domeLights` memo (:975-982) with a rig accessor (same name keeps every consumer diff minimal):

```ts
  // Scene light rig. defaultLights() guard covers snapshots that predate the
  // rig (import of a raw pre-rig snapshot bypasses the storage migration).
  const domeLights: LightInstance[] = useMemo(
    () => (design.lights && design.lights.length > 0 ? design.lights : defaultLights()),
    [design.lights],
  );
```

3. `fillRender` memo: delete the `lightAzimuth`, `lightElevation`, `lightAngle`, and `lightColor` entries (:909, :910, :920, :930) and their uses:
   - `aquaPaint` (:944): `const rad = (fillRender.lightAngle * Math.PI) / 180;` → `const rad = (domeLights[0]!.az * Math.PI) / 180;` — add `domeLights` to that memo's dependency array; also update the body-stops dependency list at :968 (`fillRender.lightAngle` → `domeLights`).
   - `litBevel` memo (:1035-1038): the lights mapping becomes a pass-through — each light keeps its own color:

```ts
    const lights = domeLights;
```

   (delete the `.map` that forced `i === 0 ? fillRender.lightColor : '#ffffff'`).

4. `LitBevelLight` (litBevelShading.ts:11-16) and `LightInstance` are structurally identical — `computeStops(region, domeLights, …)` typechecks as-is. Do NOT change `litBevelShading.ts`; keep its local interface (the shading module stays decoupled from lab state types).

- [ ] **Step 4: Remove dead descriptors from `src/controls.ts`**

Delete these four descriptor lines from the fill controls:
- `:37` `lightAngle` (aqua)
- `:49` `lightAzimuth`
- `:50` `lightElevation`
- `:71` `lightColor` ("Key light color")

Leave the `Dome` header (:47) — it still fronts `bevelWidth`.

- [ ] **Step 5: Typecheck + suite**

Run: `npm run typecheck && npx vitest run`
Expected: clean + all pass. Grep for leftovers: `grep -rn "lightAzimuth\|lightElevation\|lightAngle\|lightColor" src/` — remaining hits must be inside `lightRig.ts`/`lightRig.test.ts` (migration reads the legacy keys) only.

- [ ] **Step 6: Commit**

```bash
git add speech-balloons/src/SpeechBalloon.tsx speech-balloons/src/controls.ts speech-balloons/src/litBevelShading.test.ts
git commit -m "feat(speech-balloons): renderer consumes scene light rig; per-light color in lit-bevel"
```

---

### Task 4: `LightsPanel` block

**Files:**
- Create: `src/LightsPanel.tsx`
- Modify: `src/Lab.tsx` (right panel, :701-748)

No unit tests for this task — it's declarative JSX over labkit rows (the lab's convention: panels are browser-verified, logic is unit-tested). Behavior is covered in Task 6.

- [ ] **Step 1: Create `src/LightsPanel.tsx`**

```tsx
import { PropertyList, PropertyPanel, SliderRow, ColorRow } from '@weasel-js/labkit';
import type { LightInstance } from './types';
import { MAX_LIGHTS, MIN_LIGHTS, newLight } from './lightRig';

interface Props {
  lights: LightInstance[];
  onChange: (next: LightInstance[]) => void;
  showHandles: boolean;
  onShowHandles: (show: boolean) => void;
}

export function LightsPanel({ lights, onChange, showHandles, onShowHandles }: Props) {
  const update = (i: number, patch: Partial<LightInstance>) =>
    onChange(lights.map((l, k) => (k === i ? { ...l, ...patch } : l)));

  return (
    <PropertyPanel title="Lights">
      <label className="sb-checkbox sb-lights-handles-toggle">
        <input
          type="checkbox"
          checked={showHandles}
          onChange={(e) => onShowHandles(e.target.checked)}
        />
        <span>Show handles</span>
      </label>
      {lights.map((light, i) => (
        <section key={i} className="sb-light-group">
          <header className="sb-light-group__head">
            <h4 className="sb-light-group__title">Light {i + 1}</h4>
            <button
              type="button"
              className="sb-light-group__remove"
              disabled={lights.length <= MIN_LIGHTS}
              onClick={() => onChange(lights.filter((_, k) => k !== i))}
              title={lights.length <= MIN_LIGHTS ? 'At least one light is required' : 'Remove light'}
              aria-label={`Remove light ${i + 1}`}
            >
              ✕
            </button>
          </header>
          <PropertyList pack="pairs">
            <SliderRow label="Azimuth" value={light.az} min={0} max={359} step={1} unit={<sup>°</sup>}
              onChange={(v) => update(i, { az: v })} />
            <SliderRow label="Elevation" value={light.el} min={0} max={90} step={1} unit={<sup>°</sup>}
              onChange={(v) => update(i, { el: v })} />
            <SliderRow label="Intensity" value={light.intensity} min={0} max={2} step={0.02}
              onChange={(v) => update(i, { intensity: v })} />
            <ColorRow label="Color" value={light.color}
              onChange={(rgb) => update(i, { color: rgb })} />
          </PropertyList>
        </section>
      ))}
      <button
        type="button"
        className="sb-lights-add"
        disabled={lights.length >= MAX_LIGHTS}
        onClick={() => onChange([...lights, newLight()])}
      >
        + Add light
      </button>
    </PropertyPanel>
  );
}
```

Check `ColorRow`'s prop shape against its use in `Lab.tsx:631-637` (it takes `value={splitColor(...).rgb}`) — if it requires the split form, mirror that: `value={light.color}` may need `splitColor(light.color).rgb` with `combineColor` in the handler. Match whatever `Lab.tsx` does; light colors have no alpha.

Add minimal styles to `src/styles.css` (follow its existing class naming; no inline styles beyond what labkit rows do):

```css
.sb-light-group__head { display: flex; align-items: center; justify-content: space-between; }
.sb-light-group__title { margin: 0; font-size: 12px; text-transform: uppercase; opacity: 0.7; }
.sb-light-group__remove { background: none; border: none; cursor: pointer; color: inherit; opacity: 0.6; }
.sb-light-group__remove:disabled { opacity: 0.2; cursor: default; }
.sb-lights-add { width: 100%; margin-top: 6px; }
.sb-lights-handles-toggle { display: flex; gap: 6px; margin-bottom: 8px; }
```

- [ ] **Step 2: Mount it in `Lab.tsx`**

Import: `import { LightsPanel } from './LightsPanel';`

In the right panel, between `</div>` closing `sb-tail-minimap-wrap` (:717) and the Tails `<EffectLayerStack>` (:718), insert:

```tsx
            <LightsPanel
              lights={design.lights}
              onChange={(next) => setDesign((d) => ({ ...d, lights: next }))}
              showHandles={runtime.showLightHandles !== false}
              onShowHandles={(show) => setRuntime((r) => ({ ...r, showLightHandles: show }))}
            />
```

- [ ] **Step 3: Typecheck + eyeball**

Run: `npm run typecheck`
Expected: clean.

Run: `npm run dev -- --port 5180` (background). Open http://localhost:5180 — Lights panel shows two lights; dragging Azimuth on Light 1 re-aims the shading in dome and lit-bevel modes; add/remove works; remove disabled at one light; add disabled at six.

- [ ] **Step 4: Commit**

```bash
git add speech-balloons/src/LightsPanel.tsx speech-balloons/src/Lab.tsx speech-balloons/src/styles.css
git commit -m "feat(speech-balloons): Lights panel — add/remove/edit scene lights"
```

---

### Task 5: On-canvas gizmos

**Files:**
- Create: `src/lightGizmo.ts`
- Create: `src/lightGizmo.test.ts`
- Modify: `src/SpeechBalloon.tsx` (Props :32-38, debug overlay light math :1420-1468, JSX :1911+)
- Modify: `src/Lab.tsx` (:674-680)

- [ ] **Step 1: Write failing tests for the projection round-trip**

`src/lightGizmo.test.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { discPosition, pointerToAzEl } from './lightGizmo';

const cx = 100, cy = 80, dist = 150;

describe('light gizmo projection', () => {
  it('discPosition matches the overlay lollipop math', () => {
    // az 0, el 0: disc sits on the +x ground ring, no lift.
    expect(discPosition(0, 0, cx, cy, dist)).toEqual({ x: cx + dist, y: cy });
    // el 90: ground collapses to center, disc lifted by 0.6·dist.
    const top = discPosition(123, 90, cx, cy, dist);
    expect(top.x).toBeCloseTo(cx, 6);
    expect(top.y).toBeCloseTo(cy - 0.6 * dist, 6);
  });

  it.each([
    [0, 0], [0, 45], [0, 90],
    [90, 30], [180, 45], [270, 55], [315, 80], [45, 10],
  ])('round-trips az=%i el=%i', (az, el) => {
    const p = discPosition(az, el, cx, cy, dist);
    const back = pointerToAzEl(p.x, p.y, cx, cy, dist);
    expect(back.el).toBeCloseTo(el, 1);
    // az is undefined at el=90 (disc sits on the vertical axis) — skip there.
    if (el < 89) {
      const azDiff = Math.abs(((back.az - az + 540) % 360) - 180);
      expect(azDiff).toBeCloseTo(0, 1);
    }
  });

  it('clamps pointers outside the reachable annulus', () => {
    // Way outside the ground ring → el clamps to 0, az follows the pointer.
    const far = pointerToAzEl(cx + 10 * dist, cy, cx, cy, dist);
    expect(far.el).toBe(0);
    expect(far.az).toBeCloseTo(0, 1);
    // The canvas center is genuinely reachable: at az 90° the lift term
    // cancels the ground offset when tan(el) = 1/0.6 → el ≈ 59.04°.
    const center = pointerToAzEl(cx, cy, cx, cy, dist);
    expect(center.az).toBeCloseTo(90, 1);
    expect(center.el).toBeCloseTo(59.04, 1);
  });
});
```

- [ ] **Step 2: Run tests, confirm they fail**

Run: `npx vitest run src/lightGizmo.test.ts`
Expected: FAIL — module not found.

- [ ] **Step 3: Implement `src/lightGizmo.ts`**

The forward projection copies the overlay math at `SpeechBalloon.tsx:1425-1443`: ground point at `dist·cos(el)` along the azimuth, disc lifted screen-up by `0.6·dist·sin(el)`. The inverse solves the one-unknown equation `u² + (v + 0.6·sin el)² = cos²el` (u, v = pointer offset / dist) for el by bisection, then recovers az.

```ts
// Forward/inverse projection for the draggable light gizmos. Forward mirrors
// the debug-overlay lollipop (SpeechBalloon debugOverlay memo): ground point
// at dist·cos(el) along az, disc lifted by 0.6·dist·sin(el) in screen −y.
// Inverse: with u = (px−cx)/dist, v = (py−cy)/dist,
//   u = cos(el)·cos(az)
//   v = cos(el)·sin(az) − 0.6·sin(el)
// Eliminating az: f(el) = u² + (v + 0.6·sin el)² − cos²el = 0. f may dip
// for v < 0 (pointer above center) but any dip happens while f < 0 and f
// then rises monotonically to f(90°) = u² + (v+0.6)² ≥ 0, so f crosses zero
// exactly once and bisection between the sign change is safe.
const LIFT = 0.6;

export function discPosition(
  azDeg: number, elDeg: number,
  cx: number, cy: number, dist: number,
): { x: number; y: number } {
  const az = (azDeg * Math.PI) / 180;
  const el = (elDeg * Math.PI) / 180;
  const ce = Math.cos(el);
  return {
    x: cx + dist * ce * Math.cos(az),
    y: cy + dist * ce * Math.sin(az) - dist * LIFT * Math.sin(el),
  };
}

export function pointerToAzEl(
  px: number, py: number,
  cx: number, cy: number, dist: number,
): { az: number; el: number } {
  const u = (px - cx) / dist;
  const v = (py - cy) / dist;
  const f = (elRad: number) => {
    const s = Math.sin(elRad);
    const c = Math.cos(elRad);
    return u * u + (v + LIFT * s) * (v + LIFT * s) - c * c;
  };
  const lo0 = 0;
  const hi0 = Math.PI / 2;
  let el: number;
  if (f(lo0) >= 0) {
    el = 0;         // outside the ground ring
  } else if (f(hi0) <= 0) {
    el = Math.PI / 2; // inside the innermost reach (at/near center)
  } else {
    let lo = lo0, hi = hi0;
    for (let i = 0; i < 40; i++) {
      const mid = (lo + hi) / 2;
      if (f(mid) < 0) lo = mid; else hi = mid;
    }
    el = (lo + hi) / 2;
  }
  const az = Math.atan2(v + LIFT * Math.sin(el), u);
  return {
    az: ((az * 180) / Math.PI + 360) % 360,
    el: Math.min(90, Math.max(0, (el * 180) / Math.PI)),
  };
}
```

- [ ] **Step 4: Run tests, confirm they pass**

Run: `npx vitest run src/lightGizmo.test.ts`
Expected: PASS. If the el=0 clamp case fails on az: at el=0 the equation degenerates gracefully (`v + 0` → plain atan2) — check the test's expectation before touching the solver.

- [ ] **Step 5: Interactive layer in `SpeechBalloon.tsx`**

1. Extend Props (:32-38):

```ts
interface Props {
  design: DesignState;
  runtime: RuntimeState;
  zoom?: number;
  onShadingItems?: (items: ShadingItem[]) => void;
  highlightedShadingId?: string | null;
  // Gizmo drag: patch light i's direction. Presence of the callback +
  // runtime.showLightHandles gates the interactive layer.
  onUpdateLight?: (index: number, patch: { az: number; el: number }) => void;
}
```

2. Add a `lightHandles` memo next to `debugOverlay` — same positioning inputs but independent of `runtime.domeDebug`, active for every fill mode that shades (all four):

```ts
  const lightHandles = useMemo(() => {
    if (!onUpdateLight || runtime.showLightHandles === false) return null;
    if (!fillEffect) return null;
    const bb = bareBaseBBox(sampler);
    if (bb.w <= 0 || bb.h <= 0) return null;
    const cx = bb.x + bb.w / 2;
    const cy = bb.y + bb.h / 2;
    const dist = 0.75 * Math.max(bb.w, bb.h);
    return {
      cx, cy, dist,
      discs: domeLights.map((l, i) => ({
        ...discPosition(l.az, l.el, cx, cy, dist),
        r: 3 + 5 * Math.cos((l.el * Math.PI) / 180),
        groundX: cx + dist * Math.cos((l.el * Math.PI) / 180) * Math.cos((l.az * Math.PI) / 180),
        groundY: cy + dist * Math.cos((l.el * Math.PI) / 180) * Math.sin((l.az * Math.PI) / 180),
        color: l.color,
        index: i,
      })),
    };
  }, [onUpdateLight, runtime.showLightHandles, fillEffect, sampler, domeLights]);
```

(Import `discPosition`, `pointerToAzEl` from `./lightGizmo`. Check the actual name of the fill-effect variable used at :1027 — `fillRender.mode` gating also works: `if (fillRender.mode === undefined)` is never true, so gate on `fillEffect` existing; find its declaration near the `fillRender` memo and reuse it.)

3. Render the handles as the LAST child of the root `<svg>` (on top of everything, after the debug overlay group). Pointer handling uses SVG pointer capture — no document listeners:

```tsx
      {lightHandles && (
        <g className="sb-light-handles">
          {lightHandles.discs.map((d) => (
            <g key={d.index}>
              <line
                x1={d.groundX} y1={d.groundY} x2={d.x} y2={d.y}
                stroke="#ffd54a" strokeWidth={1} strokeDasharray="2 3" opacity={0.4}
                pointerEvents="none"
              />
              <circle
                cx={d.x} cy={d.y} r={d.r + 4}
                fill="transparent"
                stroke="#ffd54a" strokeWidth={1.5} strokeOpacity={0.9}
                style={{ cursor: 'grab' }}
                onPointerDown={(e) => {
                  e.currentTarget.setPointerCapture(e.pointerId);
                  e.preventDefault();
                }}
                onPointerMove={(e) => {
                  if (!e.currentTarget.hasPointerCapture(e.pointerId)) return;
                  const svg = e.currentTarget.ownerSVGElement!;
                  const ctm = svg.getScreenCTM();
                  if (!ctm) return;
                  const pt = new DOMPoint(e.clientX, e.clientY).matrixTransform(ctm.inverse());
                  const { az, el } = pointerToAzEl(pt.x, pt.y, lightHandles.cx, lightHandles.cy, lightHandles.dist);
                  onUpdateLight?.(d.index, { az: Math.round(az), el: Math.round(el) });
                }}
                onPointerUp={(e) => e.currentTarget.releasePointerCapture(e.pointerId)}
              />
              <circle cx={d.x} cy={d.y} r={d.r} fill={d.color} stroke="#111" strokeWidth={0.75} pointerEvents="none" />
            </g>
          ))}
        </g>
      )}
```

Note the fill disc uses the light's own `color` — the handle doubles as a color swatch. The `style={{ cursor: 'grab' }}` inline style is allowed here: cursor-on-SVG-element has no class-based equivalent in this codebase's stylesheet structure — actually it does; prefer adding `.sb-light-handles circle { cursor: grab; }` to `src/styles.css` and drop the inline style.

4. Undo: nothing to do — Lab's 300ms settle debounce coalesces the whole drag into one undo snapshot.

- [ ] **Step 6: Pass the callback from `Lab.tsx`**

At the `<SpeechBalloon>` call site (:674-680) add:

```tsx
                onUpdateLight={(i, patch) =>
                  setDesign((d) => ({
                    ...d,
                    lights: d.lights.map((l, k) => (k === i ? { ...l, ...patch } : l)),
                  }))
                }
```

- [ ] **Step 7: Typecheck + suite + hands-on check**

Run: `npm run typecheck && npx vitest run`
Expected: clean + pass.

In the dev server: drag a disc — shading re-aims live in all four modes (aqua: azimuth only, elevation drag is visually inert there — expected); pull toward center = higher elevation; one ⌘Z undoes the whole drag; toggling "Show handles" off hides the layer.

- [ ] **Step 8: Commit**

```bash
git add speech-balloons/src/lightGizmo.ts speech-balloons/src/lightGizmo.test.ts speech-balloons/src/SpeechBalloon.tsx speech-balloons/src/Lab.tsx speech-balloons/src/styles.css
git commit -m "feat(speech-balloons): draggable on-canvas light gizmos"
```

---

### Task 6: Browser verification + docs

**Files:**
- Modify: `HANDOFF.md`
- Create: `screenshots-clean/lightrig-*.png` (via Playwright MCP)

- [ ] **Step 1: Dev server + Playwright sweep**

With the dev server on port 5180, drive the browser through the spec's verification list (rectangle 280×140 + pointed tail unless noted). Screenshot each keeper to `screenshots-clean/`:

1. `lightrig-01-three-tinted.png` — lit-bevel, add a 3rd light, colors red/white/blue, distinct az: three tinted contributions visible; Shading layers panel lists exactly "Light 1", "Light 2", "Light 3".
2. `lightrig-02-dome-three.png` — dome mode, same rig: additive wedges from all three lights.
3. `lightrig-03-brdf-third-light.png` — BRDF: adding the 3rd light adds a visible diffuse lobe.
4. `lightrig-04-aqua-key.png` — aqua: dragging light 1's azimuth re-aims the gradient; adding lights changes nothing.
5. `lightrig-05-gizmo-drag.png` — mid-drag or post-drag shot showing the handle and re-aimed shading; verify pull-toward-center raises elevation (shading flattens toward overhead), and one ⌘Z restores the pre-drag state.
6. Handles-off: toggle "Show handles" — layer disappears (no screenshot needed, just verify).
7. Two-light default regression: fresh scene (localStorage cleared) renders identically to `screenshots-clean/concave-07-regression.png` setup — eyeball band structure, default rig is the same pair.
8. Migration: in DevTools, seed `localStorage['lk:speech-balloon-lab-v12:workspaces']` with a pre-rig workspace (copy a current one, delete `lights` from config, add `lightAzimuth: 300, lightElevation: 40, lightColor: '#ff0000'` to the fill params), reload: Lights panel shows light 1 az 300 / el 40 / red + light 2 az 120; render matches what those params produced before this feature (key light aimed 300°).
9. Console: 0 errors / 0 warnings across the session.

- [ ] **Step 2: Update `HANDOFF.md`**

Add a "Recent work" section at the top describing: scene-level rig on DesignState, migration semantics, per-light color in lit-bevel, BRDF-already-multi-light discovery, panel + gizmos, test count. Trim the concave-rim section to a one-paragraph pointer at its spec/plan (it's shipped and stable).

- [ ] **Step 3: Final suite + commit**

Run: `npm run typecheck && npx vitest run`
Expected: clean; ~137+ tests pass.

```bash
git add speech-balloons/HANDOFF.md speech-balloons/screenshots-clean/lightrig-*.png
git commit -m "feat(speech-balloons): light rig browser-verified; handoff updated"
```

Do NOT push — pushing triggers the Pages deploy; that's the user's call.

---

## Self-review notes

- **Spec coverage:** data model (T1), migration (T1/T2), renderer + per-light color + controls removal (T3), panel + Show handles in RuntimeState (T4), gizmos + projection + one-undo-drag (T5), testing + screenshots + docs (T6). Max-6/min-1 enforced in panel buttons (T4); gizmo count follows the list. "Clicking a gizmo highlights its panel group" from the spec is dropped as YAGNI for this pass — the handle-as-color-swatch makes identification obvious; note the cut in HANDOFF.
- **Types:** `LightInstance` defined once in `types.ts`; `lightRig.ts` re-exports nothing type-shaped; `litBevelShading.ts` keeps its structural `LitBevelLight` twin on purpose.
- **No placeholder steps:** every code step carries the code; the two "adapt to the file's fixtures" notes (T3 step 1, ColorRow prop shape in T4) are explicit adaptation points with the fallback spelled out.
